<UserControl x:Class="ModuleA.Views.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="18" Margin="5"/>
        <TextBlock Text="{Binding Counter}" FontSize="18" Margin="5"/>
        <Button Command="{Binding UnlockCounterCommand}" Content="Unlock Counter" Margin="5"/>
        <Button Command="{Binding LockCounterCommand}" Content="Lock Counter" Margin="5"/>
    </StackPanel>
</UserControl>
